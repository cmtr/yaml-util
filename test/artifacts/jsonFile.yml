---
input:
  negativeTest: should be unchanged
  simple:
    asString: $JsonFile ./simpleJsonFile.json
    asObject:
      $JsonFile: ./simpleJsonFile.json
  advanced:
    asString: $JsonFile ./advancedJsonFile.json
    asObject:
      $JsonFile: ./advancedJsonFile.json
  # TODO - Incorrect formating
  # TODO - Reference to file that does not exist
output:
  negativeTest: should be unchanged
  simple:
    asString: &simpleJson
      key: value
      nestedObject:
        key: value
      array:
        - one
        - two
        - three
    asObject: *simpleJson
  advanced:
    asString: &advancedJson
      key:
        nested:
          fixed:
            lorem: ipsum
            int: 12
            float: 0.2
          contextual:
            jsonAsString: *simpleJson
            jsonAsObject: *simpleJson
            jsonAsObjectAlt: *simpleJson
        array:
          - *simpleJson
          - *simpleJson
          - *simpleJson
    asObject: *advancedJson